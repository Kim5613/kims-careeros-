---
name: 交接笔记-2026-07-01
description: v1.0 功能封版，准备上线部署
metadata:
  type: project
  handoff: SESSION_END
---

# 交接笔记：Kim's CareerOS v1.0 上线前

## 当前状态

v1.0 功能封版，代码在 `D:\AI\项目\kims-careeros`，本地运行正常（`npm run build && npm run start` → localhost:3000）。

## 上线步骤（明天按顺序操作）

### 1. 阿里云实名认证
- 登录阿里云控制台
- 完成实名认证（如果还没做）

### 2. 购买云 RDS PostgreSQL
- 阿里云控制台 → 云数据库 RDS → 创建实例
- 规格：1核1G（最低配）
- 数据库：PostgreSQL
- 创建 `hr_platform` 数据库
- 创建用户 `hr_user`，密码设为 `Kim2026Secure`
- 获取**内网连接地址**（服务器走内网）或**公网地址**

### 3. 服务器部署
- IP：139.196.159.68
- 系统：Ubuntu 24.04，已装 Node.js / Nginx / PM2 / Docker
- 登录：阿里云控制台 → 轻量应用服务器 → 远程连接 → Workbench
- 执行：
  ```
  sudo -i
  cd /opt
  git clone https://github.com/Kim5613/kims-careerros-.git hr-platform
  cd hr-platform
  ```
- 如果 GitHub 超时，用镜像：
  ```
  git clone https://ghproxy.cc/https://github.com/Kim5613/kims-careerros-.git /opt/hr-platform
  ```
- 修改 `.env` 里的 `DATABASE_URL` 为云 RDS 地址
- 然后：
  ```
  npm install
  npx prisma db push
  npm run build
  pm2 start npm --name "hr-platform" -- start
  pm2 save
  pm2 startup
  ```

### 4. Nginx 反代
- 配置参考 `PROGRESS.md` 第四步
- 检查：`nginx -t && systemctl restart nginx`
- 访问 `http://139.196.159.68` 验证

### 5. 域名 + HTTPS（等备案通过后）
- 阿里云 DNS：A 记录 `kimstar.cn` → `139.196.159.68`
- HTTPS：`certbot --nginx -d kimstar.cn`

## 关键信息

- 服务器 IP：139.196.159.68
- 数据库：hr_platform / hr_user / Kim2026Secure
- GitHub：https://github.com/Kim5613/kims-careerros-
- 域名：kimstar.cn（备案中）
- 登录密码：111
